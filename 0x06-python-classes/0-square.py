#!/usr/bin/python3

class Square:
    """The summary line for a class docstring should fit on one line.

    public attributes, document here.
    in an ``Attributes`` section follow same fmt as a
    funct's ``Args`` section. Altly, attrs may be doced inline
    inline w the attr's declaration (see __init__ method below).

    Props created with the ``@property`` decorator should be documented
    in the property's getter method.

    Attributes:
        attr1 (str): Description of `attr1`.
        attr2 (:obj:`int`, optional): Description of `attr2`.

    """
    pass
